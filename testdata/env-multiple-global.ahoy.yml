ahoyapi: v2
env:
  - ./.env
  - path: ./.env.doesnotexist
    optional: true
  - ./.env.multiple
commands:
  test-global-multiple:
    cmd: echo $GLOBAL

  test-cmd-multiple-override:
    cmd: echo $SECOND_COMMAND
    env: .env.cmd
