ahoyapi: v2
commands:
  show-ahoy-cmd:
    cmd: echo $AHOY_CMD

  show-ahoy-command-name:
    cmd: echo $AHOY_COMMAND_NAME

  show-both:
    cmd: echo "$AHOY_CMD $AHOY_COMMAND_NAME"

  ahoy-cmd-is-set:
    cmd: test -n "$AHOY_CMD"

  ahoy-cmd-is-executable:
    cmd: test -x "$AHOY_CMD"
